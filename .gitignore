# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node (Next.js app)
node_modules/
.next/
.turbo/
dist/
next-env.d.ts

# Env / secrets
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated sample data — large binaries; regenerate from tools/
data/sample-colas/pdfs/
data/sample-colas/fail/images/

# Memory directory is private per-machine
memory/
*.tsbuildinfo

# Knowledge base file-backed prototype store (admin-uploaded reference
# material per NFR-4 — no applicant PII — but still gitignored so the
# repo doesn't carry per-machine indexed corpora).
.data/
